name: Notify on PR Creation

on:
  pull_request:
    types: [opened]

jobs:
  notify-email:
    runs-on: ubuntu-latest

    steps:
      - name: Send email notification
        uses: dawidd6/action-send-mail@v3
        with:
          to: "jayant.jose@gmail.com"
          subject: "New Pull Request Created"
          body: |
            A new pull request has been created!
            Title: ${{ github.event.pull_request.title }}
            URL: ${{ github.event.pull_request.html_url }}
          from: "admin@omnitailtools.com"
          server_address: "omnitailtools.com"
          server_port: 465
          username: "admin@omnitailtools.com"
          password: "zwzF*!Brhluo"
          secure: true
      - name: Confirm Email Sent
        run: echo "Email action completed"